
## 安装思路

### 安装过程

尤其需要注意，需要手动修改 `install_minicpm_sala.sh` 源码，把里面的 py312 都换成 py310。

```
cd autodl-tmp

pip install uv modelscope
mkdir -p ./models
cd models
modelscope download --model OpenBMB/MiniCPM-SALA --local_dir ./models/MiniCPM-SALA

git clone https://kkgithub.com/OpenBMB/sglang.git
cd sglang
git checkout minicpm_sala
export UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple
export PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple
bash install_minicpm_sala.sh

source /root/autodl-tmp/models/sglang/sglang_minicpm_sala_env/bin/activate
git config --global --add safe.directory /root/autodl-tmp/models/sglang
cd /root/autodl-tmp/models/SOAR-Toolkit
uv pip install --no-deps -e /root/autodl-tmp/models/sglang/python

cd /root/autodl-tmp/models
source prepare_env.sh
bash prepare_model.sh --input /root/autodl-tmp/models/MiniCPM-SALA --output /root/autodl-tmp/models/MiniCPM-SALA-w4a16

python3 -m sglang.launch_server \
  --model-path /root/autodl-tmp/models/MiniCPM-SALA-w4a16 \
  --host 0.0.0.0 \
  --port 30000 \
  $SGLANG_SERVER_ARGS

```

```
cd ~

git clone https://github.com/OpenBMB/SOAR-Toolkit.git
cd SOAR-Toolkit
source /root/autodl-tmp/models/sglang/sglang_minicpm_sala_env/bin/activate
python3 eval_model.py \
  --api_base http://127.0.0.1:30000 \
  --model_path /root/autodl-tmp/models/MiniCPM-SALA-w4a16 \
  --data_path eval_dataset/perf_public_set.jsonl \
  --concurrency 8
```
### 修正源码

由于 prompt 的独特路径，我们需要手动修改 sglang 的源码。

具体来说，就是 sglang 里面要求每次询问是一个 batch，但是在实际上，prompt 每次只会有一个量询问，所以我们需要伪装为 batch，也就是 `[query]`，在外层加一层列表。

具体的，在 `io_struct.py` 里面，把

```python
elif self.input_ids is not None:
	if len(self.input_ids) == 0:
		raise ValueError("input_ids cannot be empty.")
	if isinstance(self.input_ids[0], int):
		self.is_single = True
		self.batch_size = 1
	else:
		self.is_single = False
		self.batch_size = len(self.input_ids)
	self.input_embeds = None
```

改为：

```python
elif self.input_ids is not None:
	# MiniCPM-SALA compatibility:
	# OpenAI chat endpoint may pass transformers BatchEncoding:
	# {"input_ids": [...], "attention_mask": [...]}
	# Convert it back to raw token ids only; do not change token content.
	if hasattr(self.input_ids, "data") and isinstance(getattr(self.input_ids, "data"), dict):
		self.input_ids = self.input_ids.data.get("input_ids", self.input_ids)
	elif isinstance(self.input_ids, dict) and "input_ids" in self.input_ids:
		self.input_ids = self.input_ids["input_ids"]
		
	if len(self.input_ids) == 0:
		raise ValueError("input_ids cannot be empty.")
	if isinstance(self.input_ids[0], int):
		self.is_single = True
		self.batch_size = 1
	else:
		self.is_single = False
		self.batch_size = len(self.input_ids)
	self.input_embeds = None
```

### 库说明

基本的库在 `prepare_env.sh` 里面已经说明，现在说明一些比较奇怪的版本问题。

#### gptqmodel

其实这个不是很清楚，可能因为它的扩展比较多，依赖也比较复杂，为了编译它的扩展，就要开 `--no-binary gptqmodel`。

当时应该是有一个扩展，如果不先编译的话，后续编译极慢（会直接崩掉），所以就加了这个命令。但是在后续测试里面，那个扩展好像还是没有编译，又用其他手段把它预编译了。
#### flash_attn

线上环境比较特殊，是 py310 + torch2.91，而并没有这个版本的 flash_attn。

我只好另辟蹊径，先对环境进行降级，把 torch 变为 2.80，量化之后，不再需要 flash_attn，于是又要升回去（实际表明，后续需要 torch2.91 环境），也就是 
`uv pip install torch==2.9.1 torchvision==0.24.1 torchaudio==2.9.1 --index-url https://download.pytorch.org/whl/cu128`

另外 flash_attn 不能直接去 install（可能因为版本多、依赖复杂），需要自己先本地下轮子，之后编译。
#### flashinfer

量化路径需要 `flashinfer`，而它的库名字是 `flashinfer-python`，这里有要求用低版本 `flashinfer-python==0.5.3`。另外由于我们要量化，所以不能用普通的 FP8，需要改为有 `scaling` 等信息的 MXFP8，也就是需要设置路径 `export FLASHINFER_FP8_MODE="mxfp8"`。

MXFP8 外，还有 E4M3 和 E5M2。

其他不需要设置路径，可能是因为在 W4A16 里面，其他的可以从文件里面读出来需要什么格式。
#### torchao

需要降级，由于只有在推理阶段有用，有貌似与 torch 有点关系，所以我是在 torch 升级升回去之后再降级的。

`uv pip install --force-reinstall --no-deps torchao==0.8.0`

`--no-deps` 应该是为了防止其他东西连带着降级。

### patch 说明

#### head_first = False

好像什么默认 `head_first = False`，但是需要改掉，就用 patch 把这几个东西都删了。

同时好像还要整治 `writable HF cache` 什么的，我没看懂。

#### float16 -> bfloat16

这个也怪怪的，就是在统一使用的时候，会有莫名其妙的类型不对应错误，所以需要 patch 把里面所有的类型换掉。

#### 路径说明

嗯，可能原本环境很多路径都不认识，所以需要自己去定义环境。

都是 ai 写的，也不敢删。

### 其余教训

#### true_sequential=True

这个是用于安全检查的，如果贸然改为 `False` 可能会出一些问题，导致精度暴跌。

## 模型转载

先检查一下盘的内存大小，有时候可能其他盘的内存才够用。

加速

source /etc/network_turbo

关闭加速

unset http_proxy && unset https_proxy

先要装载模型

pip install uv modelscope
mkdir -p ./models
modelscope download --model OpenBMB/MiniCPM-SALA --local_dir ./models/MiniCPM-SALA

之后要装一系列库

国内源：git clone https://kkgithub.com/OpenBMB/sglang.git
git clone https://github.com/OpenBMB/sglang.git
cd sglang
git checkout minicpm_sala

然后还要重新设置 nvdia 路径，装 g++ 编译器（可能不需要？）

最后这个即可

切换清华源：export UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple
export PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple

bash install_minicpm_sala.sh

然后是启动服务

source sglang_minicpm_sala_env/bin/activate

python3 -m sglang.launch_server \
  --model-path /home/ubuntu/models/MiniCPM-SALA \
  --disable-radix-cache \
  --attention-backend flashinfer \
  --chunked-prefill-size 32768 \
  --host 0.0.0.0 \
  --port 30000

python3 -m sglang.launch_server \
  --model-path /root/autodl-tmp/models/MiniCPM-SALA-w4a16 \
  --disable-radix-cache \
  --attention-backend flashinfer \
  --chunked-prefill-size 32768 \
  --host 0.0.0.0 \
  --port 30000

python3 -m sglang.launch_server \
  --model-path /root/autodl-tmp/models/MiniCPM-SALA-w4a16 \
  --host 0.0.0.0 \
  --port 30000 \
  --log-level info \
  --disable-radix-cache \
  --attention-backend minicpm_flashinfer \
  --chunked-prefill-size 8192 \
  --skip-server-warmup \
  --dense-as-sparse \
  --quantization gptq_marlin \
  --dtype float16 \
  --disable-cuda-graph \
  --mem-fraction-static 0.80


然后是评测准确率部分

cd ~

git clone https://github.com/OpenBMB/SOAR-Toolkit.git

cd SOAR-Toolkit

source /home/ubuntu/sglang/sglang_minicpm_sala_env/bin/activate

source /root/autodl-tmp/models/sglang/sglang_minicpm_sala_env/bin/activate


python3 eval_model.py \
  --api_base http://127.0.0.1:30000 \
  --model_path /home/ubuntu/models/MiniCPM-SALA \
  --data_path eval_dataset/perf_public_set.jsonl \
  --concurrency 32

python3 eval_model.py \
  --api_base http://127.0.0.1:30000 \
  --model_path /root/autodl-tmp/models/MiniCPM-SALA-w4a16 \
  --data_path eval_dataset/perf_public_set.jsonl \
  --concurrency 32 \
  --num_samples 10

python3 eval_model.py \
  --api_base http://127.0.0.1:30000 \
  --model_path /root/autodl-tmp/models/MiniCPM-SALA \
  --data_path eval_dataset/perf_public_set.jsonl \
  --concurrency 8 \
  --num_samples 100



移动文件到云端


scp -P 46726 -r ./SOAR/ root@connect.westd.seetacloud.com:/root/autodl-tmp/models



解开文件夹

mv ./SOAR/* ./ && mv ./SOAR/.* ./ 2>/dev/null

配置新环境

source /root/autodl-tmp/models/sglang/sglang_minicpm_sala_env/bin/activate
source prepare_env.sh

量化

bash prepare_model.sh --input /root/autodl-tmp/models/MiniCPM-SALA --output /root/autodl-tmp/models/MiniCPM-SALA-w4a16



ssh -p 13312 root@connect.bjb1.seetacloud.com
50GOhUFWUYCp
cd autodl-tmp
cd models
rm -rf  perf_public_set.jsonl   prepare_env.sh   prepare_model.sh quantize.py  utils
mv ./SOAR/* ./ && mv ./SOAR/.* ./ 2>/dev/null
source /root/autodl-tmp/models/sglang/sglang_minicpm_sala_env/bin/activate
source prepare_env.sh


## 踩过的坑

`uv pip` 是懒编译，里面有些东西是用到再编译，用到的时候又可能因为梯子等等原因而卡住，所以最好启动全部编译的模式 `--no-binary gptqmodel`。

`flash-attn` 因为版本众多，所以最好确认版本之后用源码编译的方式。

对于每一层形态不同的模型，可能一些参数上会有不同，而 `GPTQModel` 只针对同构形态的模型进行优化，也就是说它会识别第一层模型，之后根据第一层对后续进行量化，如果有出入的话，那么最好的方法就是不对它进行量化。

`r"-:.*\.o_gate$": {}` 就是表示不对这个 `gate` 进行量化，正常来说，形态不同也就是 `gate` 上有差异，`qkv` 等等一般都是一致的，而 `gate` 又比较快，所以不量化影响不大。

flashinfer 0.5.3 fp8

跑的时候开代理

跑的时候要加参数

注意要修改 io_struct.py

sglang 源码要自己放在目录下







scp -P 19085 "C:\Users\Administrator\Desktop\SOAR.tar.gz" root@connect.westd.seetacloud.com:/root/autodl-tmp/models/

C0kCl8k/FRiU

tar -xzvf SOAR.tar.gz
